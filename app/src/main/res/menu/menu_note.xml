<?xml version="1.0" encoding="utf-8"?>
<!--用于定义应用菜单（如选项菜单、上下文菜单或子菜单）的 XML 文件-->
<!--
    <menu xmlns:android="http://schemas.android.com/apk/res/android">
        <item android:id="@[+][package:]id/resource_name"
        菜单项。可能包含 <menu> 元素（用于子菜单）。必须是 <menu> 或 <group> 元素的子元素。
              android:title="string"
              字符串资源或原始字符串形式的菜单标题。
              android:titleCondensed="string"
              字符串资源或原始字符串形式的压缩标题。此标题在正常标题过长的情况下使用。
              android:icon="@[package:]drawable/drawable_resource_name"
              用作菜单项图标的图片。
              android:onClick="method name"
              点击此菜单项时调用的方法。此方法必须在 Activity 中声明为公共方法，并接受 MenuItem 作为其唯一参数，
              该参数指示所点击的项。此方法优先于对 onOptionsItemSelected() 的标准回调。请参见页面结尾处的示例。
              android:showAsAction=["ifRoom" | "never" | "withText" | "always" | "collapseActionView"]
              指示此项应在应用栏中显示为操作项的时机和方式。菜单项只有在 Activity 包含应用栏时才能显示为操作项
              ifRoom: 只有在应用栏中有空间的情况下，才将此项放置其中。如果没有足够的空间来容纳标记为 "ifRoom" 的所有项，则 orderInCategory 值最低的项会显示为操作，其余项将显示在溢出菜单中。
              withText: 随操作项添加标题文本（由 android:title 定义）。您可以将此值与某个其他值一起作为标记集添加，用竖线 | 分隔
              always: 始终将此项放在应用栏中，除非此项必须始终显示在操作栏中，否则不要使用这个值，将多个项设置为始终显示，会导致他们与应用栏中的其他界面重叠
              never：不显示在应用栏中
              collapseActionView：
              android:actionLayout="@[package:]layout/layout_resource_name"
              用作操作视图的布局
              android:actionViewClass="class name"
              要用作操作视图的 View 的完全限定类名称。例如，"android.widget.SearchView" 表示将使用 SearchView 作为操作视图。
              android:actionProviderClass="class name"
              要用于代替操作项的 ActionProvider 的完全限定类名称。例如，"android.widget.ShareActionProvider" 表示将使用 ShareActionProvider。
              android:alphabeticShortcut="string"
              字母快捷键的字符。
              android:alphabeticModifiers=["META" | "CTRL" | "ALT" | "SHIFT" | "SYM" | "FUNCTION"]
              android:numericShortcut="string"
              数字快捷键的数字。
              android:numericModifiers=["META" | "CTRL" | "ALT" | "SHIFT" | "SYM" | "FUNCTION"]
              用于设置快捷键
              android:checkable=["true" | "false"]
              如果该项可勾选，则为“true”。
              android:checked=["true" | "false"]
              如果默认情况下该项为勾选状态，则为“true”。
              android:visible=["true" | "false"]
              如果默认情况下该项可见，则为“true”。
              android:enabled=["true" | "false"]
              如果默认情况下该项为启用状态，则为“true”。
              android:menuCategory=["container" | "system" | "secondary" | "alternative"]
              对应于 Menu CATEGORY_* 常量的值，这些常量用于定义项的优先级。有效值：
              android:orderInCategory="integer"
              在菜单组内的优先级
               />
        <group android:id="@[+][package:]id/resource name"
        一个菜单组，用于创建一组具有相同特征（例如是否可见、是否启用或是否可勾选）的项。包含一个或多个 <item> 元素。
        必须是 <menu> 元素的子元素。
               android:checkableBehavior=["none" | "all" | "single"]
               组的可勾选行为类型， 不可勾选，  多选， 单选
               android:visible=["true" | "false"]
               如果组可见，则为“true”。
               android:enabled=["true" | "false"]
               如果组为启用状态，则为“true”。
               android:menuCategory=["container" | "system" | "secondary" | "alternative"]
               android:orderInCategory="integer"
                项在类别中的默认顺序。>
            <item />
        </group>
        <item >
            <menu>
              <item />
            </menu>
        </item>
    </menu>
-->
<menu xmlns:android="http://schemas.android.com/apk/res/android">

</menu>
